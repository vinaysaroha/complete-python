# name can_not start with number
# there can be no space in name,use_ instead
# can't use any of these symbols: :''',<>/?|\()!@#$%^&*~-+
# Rules for variable_names:
# it's consider best practice that names are lower case
# Avoid using words that have special meaning in python like "list" and "str"
# Python uses dynamic typing
# this means you can reassign variable to different data types
my_name = 'vinay'
print(type(my_name))

my_name = 2
print(type(my_name))

my_name = 'rahul'
print(my_name)

a = 10
a = a+a
print(a)
